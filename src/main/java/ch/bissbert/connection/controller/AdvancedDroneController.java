package ch.bissbert.connection.controller;

import ch.bissbert.connection.Connection;

public class AdvancedDroneController extends DroneController {
    public AdvancedDroneController(Connection connection) {
        super(connection);
    }
}
