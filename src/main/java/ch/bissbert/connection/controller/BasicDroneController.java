package ch.bissbert.connection.controller;

import ch.bissbert.connection.Connection;

public class BasicDroneController extends DroneController {
    public BasicDroneController(Connection connection) {
        super(connection);
    }
}
